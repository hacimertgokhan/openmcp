{
  "name": "openmcp",
  "version": "0.1.1",
  "private": true,
  "type": "module",
  "description": "Desktop and CLI manager for OpenCode MCP servers.",
  "bin": {
    "openmcp": "./scripts/openmcp-cli.mjs"
  },
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vue-tsc --noEmit && vite build",
    "preview": "vite preview --host 127.0.0.1",
    "tauri": "tauri",
    "cli": "node ./scripts/openmcp-cli.mjs"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.9.0",
    "@vitejs/plugin-vue": "^6.0.1",
    "vue": "^3.5.24"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.9.3",
    "typescript": "~5.9.3",
    "vite": "^7.2.4",
    "vue-tsc": "^3.1.5"
  },
  "engines": {
    "node": ">=20"
  }
}

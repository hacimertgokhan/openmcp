[package]
name = "openmcp"
version = "0.1.0"
description = "Desktop manager for OpenCode MCP servers"
authors = ["OpenMCP"]
edition = "2021"

[lib]
name = "openmcp_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
dirs = "6"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tauri = { version = "2", features = [] }
